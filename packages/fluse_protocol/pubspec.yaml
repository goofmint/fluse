name: fluse_protocol
description: fluse のサーバとランタイムが交わす制御メッセージとトンネルフレームの定義・符号化・バージョンネゴシエーション。
version: 0.1.0
# pub.dev への公開は Task 7.3 でまとめて解禁する。
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace
