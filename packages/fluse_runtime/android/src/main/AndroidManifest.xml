<?xml version="1.0" encoding="utf-8"?>
<!--
  **ここには何も宣言しない。**

  fluse の宣言はすべて `src/debug/AndroidManifest.xml` にある
  （設計 §10-4）。fluse は dev_dependency で release には含まれないが、
  main に置くと構成次第で release のマニフェストにも載る。
  「入らないはず」ではなく「入れられない」形にしておく。

  完了条件は「release ビルドの Manifest に fluse の痕跡がないこと」。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
